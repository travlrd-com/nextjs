## Local Development

To, run the development server:

```bash
vercel dev
```

This command will pull the development environment variables from Vercel, so you don't need to create a `.env` file.

## Project links

